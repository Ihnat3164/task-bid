<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- Создание таблицы задач -->
    <changeSet id="create-tasks-table" author="ignat">

        <createTable tableName="tasks">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>

            <column name="title" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="TEXT"/>

            <column name="city" type="VARCHAR(255)"/>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP"/>

            <column name="author_profile_id" type="UUID">
                <constraints nullable="false" foreignKeyName="fk_task_author"
                             referencedTableName="profiles"
                             referencedColumnNames="id"/>
            </column>
        </createTable>

    </changeSet>

    <!-- Связующая таблица для навыков задачи -->
    <changeSet id="create-task-skills-table" author="ignat">

        <createTable tableName="task_skills">
            <column name="task_id" type="BIGINT">
                <constraints nullable="false" />
            </column>

            <column name="skill_id" type="BIGINT">
                <constraints nullable="false" />
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="task_skills"
                baseColumnNames="task_id"
                referencedTableName="tasks"
                referencedColumnNames="id"
                constraintName="fk_taskskills_task"/>

        <addForeignKeyConstraint
                baseTableName="task_skills"
                baseColumnNames="skill_id"
                referencedTableName="skills"
                referencedColumnNames="id"
                constraintName="fk_taskskills_skill"/>

    </changeSet>

</databaseChangeLog>
