<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="9-create-task-reviews" author="ignat">
        <createTable tableName="task_reviews">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>

            <column name="task_id" type="BIGINT">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="customer_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="executor_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="rating" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="comment" type="TEXT"/>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE task_reviews
            ADD CONSTRAINT chk_task_reviews_rating CHECK (rating &gt;= 1 AND rating &lt;= 5);
        </sql>

        <addForeignKeyConstraint
                baseTableName="task_reviews"
                baseColumnNames="task_id"
                referencedTableName="tasks"
                referencedColumnNames="id"
                constraintName="fk_task_reviews_task"/>

        <addForeignKeyConstraint
                baseTableName="task_reviews"
                baseColumnNames="customer_id"
                referencedTableName="profiles"
                referencedColumnNames="id"
                constraintName="fk_task_reviews_customer"/>

        <addForeignKeyConstraint
                baseTableName="task_reviews"
                baseColumnNames="executor_id"
                referencedTableName="profiles"
                referencedColumnNames="id"
                constraintName="fk_task_reviews_executor"/>

        <createIndex tableName="task_reviews" indexName="idx_task_reviews_executor">
            <column name="executor_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
